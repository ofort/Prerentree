{% extends "OFortPrerentreeBundle::layout.html.twig" %}
{% block title %}
    Barettes - {{ parent() }}
{% endblock %}
{% block ofortprerentree_body %}
    <h3>Liste des barettes</h3>
    <p><a href="{{ path('o_fort_prerentree_barette_add') }}">Ajouter une barette</a></p>
    <table class="table table-striped table-condensed">
        <tr>
            <td>N°</td>
            <td></td>
            <td>Nom court</td>
        </tr>
        {% for bar in barettes %}
            <tr>
                <td>{{ loop.index }}</td>
                <td>
                    <a href="#"><i class="material-icons">delete</i></a>
                    <a href="#{#{{ path('o_fort_prerentree_barette_modifiy', {'id': di.id}) }}#}"><i class="material-icons">mode_edit</i></a>
                </td>
                <td>
                    <a href="{{ path('o_fort_prerentree_barette_view', {'id': bar.id}) }}" title="{{ bar.nom }}">{{ bar.nomCourt }}</a>
                </td>           
            </tr>
            {% endfor %}
        </table>
        <p><a href="{{ path('o_fort_prerentree_barette_add') }}">Ajouter une barette</a></p>
    {% endblock %}