
{% extends "OFortPrerentreeBundle::layout.html.twig" %}

{% block title %}
  Disciplines - {{ parent() }}
{% endblock %}

{% block ofortprerentree_body %}

    <h3>Liste des filieres</h3>
    <p><a href="{{ path('o_fort_prerentree_discipline_add') }}">Ajouter une discipline</a></p>
    <table class="table table-striped table-condensed">
    	<tr>
    		<td>N°</td>
            <td></td>
    		<td>Nom</td>
            <td>Code</td>

       	</tr>
    	{% for di in disciplines %}
    		<tr>
    			<td>{{ loop.index }}</td>
                <td>
                    <a href="#{# {{ path('o_fort_prerentree_filiere_del', {'id': fi.id}) }}#} ">Supprimer</a>
                </td>
    			<td>
    				<a href="{{ path('o_fort_prerentree_discipline_modifiy', {'id': di.id}) }}">{{ di.intitule }}</a>
    			</td>
                <td>
                    <a href="#{# {{ path('o_fort_prerentree_filiere_del', {'id': fi.id}) }}#}">{{ di.code }}</a>
                </td>    		</tr>
    	{% endfor %}
    </table>

    <p><a href="{{ path('o_fort_prerentree_discipline_add') }}">Ajouter une discipline</a></p>


{% endblock %}
